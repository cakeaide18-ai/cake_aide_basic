# Copy this file to android/key.properties and fill in your values.
# Do NOT commit android/key.properties to version control.
# Absolute path to your keystore file
storeFile=/Users/bolajieyo/Documents/cake_aide_basic/android/app/keystore/upload-keystore.jks
# Keystore (store) password
storePassword=REPLACE_WITH_STORE_PASSWORD
# Alias name from `keytool -list` (case sensitive)
keyAlias=cakeaidepro
# Key (alias) password
keyPassword=REPLACE_WITH_KEY_PASSWORD
